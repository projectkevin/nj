extends ../layout

block content
  .page-header
    h3 All Jobs

  ul
    for job in jobs
      li= job.name
  form(method='post')
                | A job
                div
                    input(type='text', name='username')
                    input(type='submit', value='Log In')
